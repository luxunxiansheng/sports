{
    "name": "Sports Development",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "runArgs": [
		"--gpus=all",
		"--shm-size=10g",
		"--network=host"
	],
	"containerEnv": {
	   "http_proxy": "${localEnv:http_proxy}",
       "https_proxy": "${localEnv:https_proxy}",
       "no_proxy": "${localEnv:no_proxy}"
	},
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "ms-python.black-formatter",
                "ms-python.flake8",
                "ms-toolsai.jupyter",
                "ms-vscode.vscode-json",
                "ms-vscode.vscode-html",
                "ms-vscode.vscode-css",
                "ms-vscode.vscode-javascript",
                "RSIP-Vision.nvidia-smi-plus",
                "mhutchie.git-graph"
            ]
        }
    },
    "postCreateCommand": "git config --global http.postBuffer 524288000 && git config --global http.lowSpeedLimit 0 && git config --global http.lowSpeedTime 999999 && pip install -e . && git config --global user.name \"OrNot\" && git config --global user.email \"ornot2008@yahoo.com\" && git clone --depth 1 https://github.com/roboflow/notebooks && git clone --depth 1 https://github.com/roboflow/inference && git clone --depth 1 https://github.com/autodistill/autodistill && git clone --depth 1 https://github.com/roboflow/multimodal-maestro",
    "forwardPorts": [8000],
    "portsAttributes": {
        "8000": {
            "label": "Sports App",
            "onAutoForward": "notify"
        }
    }
}